var express = require('express');
var router = express.Router();
var fs = require('fs');
/* GET users listing. */
router.get('/', function(req, res, next) {
	 // res.render('thankyou', { title: req.body.fname ,success:true,csrfToken: req.csrfToken()});

});




module.exports = router;
